╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║                    CONFIGURAR CONEXION A SQL SERVER                          ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝


┌──────────────────────────────────────────────────────────────────────────────┐
│  📝 INSTRUCCIONES RAPIDAS                                                    │
└──────────────────────────────────────────────────────────────────────────────┘

  1. Abrir el archivo: db_config.json
  2. Modificar el campo "server" con tu nombre de servidor
  3. Guardar el archivo
  4. Ejecutar: python main.py


┌──────────────────────────────────────────────────────────────────────────────┐
│  🔧 COMO MODIFICAR db_config.json                                            │
└──────────────────────────────────────────────────────────────────────────────┘

  El archivo db_config.json contiene:

  {
      "server": "BRAD-PC",           ← CAMBIAR ESTO
      "auth_type": "Windows",        ← Dejar como está (recomendado)
      "username": "",                ← Dejar vacío si usas Windows
      "password": "",                ← Dejar vacío si usas Windows
      "database": "SOF108"           ← NO CAMBIAR
  }


┌──────────────────────────────────────────────────────────────────────────────┐
│  🔍 COMO OBTENER TU NOMBRE DE SERVIDOR                                       │
└──────────────────────────────────────────────────────────────────────────────┘

  METODO 1: Desde SQL Server Management Studio (SSMS)
  
    1. Abrir SSMS
    2. En la ventana de conexión, copiar el nombre del servidor
    3. Ejemplos: MIPC, MIPC\SQLEXPRESS, localhost

  METODO 2: Probar nombres comunes
  
    • localhost
    • .\SQLEXPRESS
    • TU-PC (tu nombre de computadora)
    • TU-PC\SQLEXPRESS
    • (local)


┌──────────────────────────────────────────────────────────────────────────────┐
│  📋 EJEMPLOS DE CONFIGURACION                                                │
└──────────────────────────────────────────────────────────────────────────────┘

  EJEMPLO 1: SQL Server Express (más común)
  
  {
      "server": ".\SQLEXPRESS",
      "auth_type": "Windows",
      "username": "",
      "password": "",
      "database": "SOF108"
  }

  EJEMPLO 2: SQL Server estándar con nombre de PC
  
  {
      "server": "MIPC",
      "auth_type": "Windows",
      "username": "",
      "password": "",
      "database": "SOF108"
  }

  EJEMPLO 3: Localhost
  
  {
      "server": "localhost",
      "auth_type": "Windows",
      "username": "",
      "password": "",
      "database": "SOF108"
  }

  EJEMPLO 4: Con autenticación SQL Server
  
  {
      "server": "localhost",
      "auth_type": "SQL",
      "username": "sa",
      "password": "tu_contraseña",
      "database": "SOF108"
  }


┌──────────────────────────────────────────────────────────────────────────────┐
│  ⚙️ TIPOS DE AUTENTICACION                                                   │
└──────────────────────────────────────────────────────────────────────────────┘

  OPCION 1: Windows (Recomendado)
  
    "auth_type": "Windows"
    "username": ""          ← Dejar vacío
    "password": ""          ← Dejar vacío
    
    ✓ Más fácil
    ✓ Más seguro
    ✓ No necesita usuario/contraseña

  OPCION 2: SQL Server
  
    "auth_type": "SQL"
    "username": "sa"        ← Usuario de SQL Server
    "password": "tu_pass"   ← Contraseña de SQL Server
    
    ⚠ Necesita usuario y contraseña
    ⚠ Debe estar habilitado en SQL Server


┌──────────────────────────────────────────────────────────────────────────────┐
│  ✅ VERIFICAR CONFIGURACION                                                  │
└──────────────────────────────────────────────────────────────────────────────┘

  Después de modificar db_config.json:

  1. Guardar el archivo
  2. Ejecutar: python main.py
  3. Si la conexión es exitosa, la aplicación se abrirá
  4. Si hay error, revisar el nombre del servidor


┌──────────────────────────────────────────────────────────────────────────────┐
│  🆘 PROBLEMAS COMUNES                                                        │
└──────────────────────────────────────────────────────────────────────────────┘

  ❌ "Cannot connect to server"
  
     Soluciones:
     1. Verificar que SQL Server está ejecutándose
        Win + R → services.msc → Buscar "SQL Server"
     
     2. Probar diferentes nombres de servidor:
        - localhost
        - .\SQLEXPRESS
        - TU-PC
        - (local)
     
     3. Verificar el nombre correcto en SSMS

  ❌ "Login failed"
  
     Soluciones:
     1. Usar autenticación Windows:
        "auth_type": "Windows"
        "username": ""
        "password": ""
     
     2. Si usas SQL Server, verificar usuario y contraseña

  ❌ "Database SOF108 not found"
  
     Solución:
     1. Abrir SSMS
     2. Ejecutar el archivo: database_schema.sql
     3. Esto creará la base de datos SOF108


┌──────────────────────────────────────────────────────────────────────────────┐
│  🔄 RECONFIGURAR DESDE LA APLICACION                                         │
└──────────────────────────────────────────────────────────────────────────────┘

  También puedes cambiar la configuración desde la aplicación:

  1. Ejecutar: python main.py
  2. Click en "⚙️ Configuración" en el menú lateral
  3. Modificar los datos
  4. Click en "Probar Conexión"
  5. Click en "Guardar y Conectar"


┌──────────────────────────────────────────────────────────────────────────────┐
│  📌 NOTAS IMPORTANTES                                                        │
└──────────────────────────────────────────────────────────────────────────────┘

  • El campo "database" siempre debe ser "SOF108"
  • Si usas autenticación Windows, deja username y password vacíos
  • El archivo db_config.json debe estar en la carpeta raíz del proyecto
  • Después de modificar, solo ejecuta: python main.py


╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║                    PASOS PARA NUEVA PERSONA:                                 ║
║                                                                              ║
║    1. Abrir db_config.json                                                   ║
║    2. Cambiar "server" por tu nombre de servidor                             ║
║    3. Guardar                                                                ║
║    4. Ejecutar: python main.py                                               ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝
